# Generated from xtask::workflows::extensions::release_version within the Cherry repository.
# Rebuild with `cargo xtask workflows`.
name: extensions::release_version
on:
  push:
    tags:
      - v**
jobs:
  call_release_version:
    permissions:
      contents: write
      pull-requests: write
    uses: cherry-industries/cherry/.github/workflows/extension_release.yml@main
    secrets:
      app-id: ${{ secrets.ZED_ZIPPY_APP_ID }}
      app-secret: ${{ secrets.ZED_ZIPPY_APP_PRIVATE_KEY }}
